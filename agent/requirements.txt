# Bỏ langchain/langgraph/chromadb/rank-bm25: toàn bộ bài giảng vừa trong context
# window nên không cần retrieval, và OpenAI SDK gọi thẳng cho streaming thật.
#
# Env cần thiết (agent/.env):
#   OPENAI_API_KEY   — bắt buộc
#   OPENAI_MODEL     — tuỳ chọn, mặc định gpt-4o-mini
#   TAVILY_API_KEY   — tuỳ chọn. Có thì search_web dùng Tavily (free 1.000 req/tháng,
#                      kết quả tối ưu cho LLM); không có thì tự dùng DuckDuckGo (không cần key).
# Web search chỉ cần httpx, không thêm dependency nào.
fastapi>=0.115
uvicorn[standard]>=0.30
openai>=1.50
python-dotenv>=1.0
pydantic>=2.7
httpx>=0.27
